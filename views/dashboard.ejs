<div class="row">
  <!-- Carrusel de Actividades Recientes -->
  <% if (actividades && actividades.length > 0) { %>
  <div class="col-12 mb-4">
    <div class="card shadow border-primary">
      <div class="card-header bg-primary text-white">
        <h5 class="mb-0"><i class="fas fa-rss me-2"></i> Actividades Recientes</h5>
      </div>
      <div class="card-body p-0">
        <div id="carouselActividades" class="carousel slide" data-bs-ride="carousel" data-bs-interval="5000">
          <% let grupos = []; %>
          <% for(let i = 0; i < actividades.length; i += 2) { %>
            <% grupos.push(actividades.slice(i, i + 2)); %>
          <% } %>

          <!-- Indicadores -->
          <div class="carousel-indicators">
            <% grupos.forEach((grupo, index) => { %>
              <button type="button" 
                      data-bs-target="#carouselActividades" 
                      data-bs-slide-to="<%= index %>" 
                      class="<%= index === 0 ? 'active' : '' %>"
                      aria-current="<%= index === 0 ? 'true' : 'false' %>"
                      aria-label="Slide <%= index + 1 %>">
              </button>
            <% }); %>
          </div>
          
          <div class="carousel-inner">
            <% grupos.forEach((grupo, index) => { %>
              <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                <div class="list-group list-group-flush">
                  <% grupo.forEach(actividad => { %>
                    <div class="list-group-item p-3">
                      <div class="d-flex">
                        <!-- Ícono -->
                        <div class="me-3">
                          <div class="rounded-circle bg-<%= actividad.color %> d-flex justify-content-center align-items-center" 
                               style="width: 48px; height: 48px; min-width: 48px;">
                            <i class="fas <%= actividad.icono %> text-white"></i>
                          </div>
                        </div>
                        
                        <!-- Contenido -->
                        <div class="flex-grow-1">
                          <div class="d-flex justify-content-between align-items-center">
                            <h6 class="mb-1">
                              <% if (actividad.usuario) { %>
                                <a href="/users/profile/<%= actividad.usuario._id %>" class="text-decoration-none">
                                  <%= actividad.usuario.nombre %> <%= actividad.usuario.apellido %>
                                </a>
                              <% } else { %>
                                Usuario
                              <% } %>
                            </h6>
                            <small class="text-muted">
                              <%= formatearFecha(actividad.fecha) %>
                            </small>
                          </div>
                          <p class="mb-1">
                            <%= actividad.mensaje %>
                          </p>
                          <div>
                            <% if (actividad.tipo === 'entrenamiento') { %>
                              <a href="/entrenamientos/<%= actividad.entidadId %>" class="btn btn-sm btn-outline-success">
                                <i class="fas fa-eye me-1"></i> Ver entrenamiento
                              </a>
                            <% } else if (actividad.tipo === 'logro') { %>
                              <span class="badge bg-warning">
                                <i class="fas fa-trophy me-1"></i> Logro
                              </span>
                            <% } else if (actividad.tipo === 'nivel') { %>
                              <span class="badge bg-primary">
                                <i class="fas fa-level-up-alt me-1"></i> Subida de nivel
                              </span>
                            <% } else if (actividad.tipo === 'torneo') { %>
                              <a href="/torneos/<%= actividad.entidadId %>" class="btn btn-sm btn-outline-primary">
                                <i class="fas fa-eye me-1"></i> Ver torneo
                              </a>
                            <% } %>
                          </div>
                        </div>
                      </div>
                    </div>
                  <% }); %>
                </div>
              </div>
            <% }); %>
          </div>
        </div>
        <div class="card-footer text-center">
          <a href="/actividades" class="btn btn-sm btn-primary">
            <i class="fas fa-rss me-1"></i> Ver todas las actividades
          </a>
        </div>
      </div>
    </div>
  </div>
  <% } %>
  
  <!-- Información del usuario -->
  <div class="col-md-12 mb-4">
    <div class="card shadow">
      <div class="card-body">
        <h2 class="card-title">Bienvenido, <%= user.nombre %> <%= user.apellido %></h2>
        <p class="card-text">Cédula: <%= user.cedula %></p>
        <p class="card-text">Tipo de Arco: <%= user.tipoArco %></p>
        <div class="mt-3">
          <a href="/users/profile" class="btn btn-outline-primary">
            <i class="fas fa-user-circle me-2"></i> Ver Mi Perfil
          </a>
        </div>
        <% if(user.isAdmin) { %>
          <div class="alert alert-info">
            <i class="fas fa-crown me-2"></i> Tienes permisos de administrador
          </div>
        <% } %>
      </div>
    </div>
  </div>

  <div class="col-md-6 mb-4">
    <div class="card shadow h-100">
      <div class="card-header bg-primary text-white">
        <h4 class="mb-0"><i class="fas fa-trophy me-2"></i> Mis Torneos</h4>
      </div>
      <div class="card-body">
        <p>Aquí se mostrarán los torneos en los que participas.</p>
        <div class="d-grid">
          <a href="/torneos/mis-torneos" class="btn btn-outline-primary">Ver Mis Torneos</a>
        </div>
      </div>
    </div>
  </div>

  <div class="col-md-6 mb-4">
    <div class="card shadow h-100">
      <div class="card-header bg-success text-white">
        <h4 class="mb-0"><i class="fas fa-plus-circle me-2"></i> Crear Torneo</h4>
      </div>
      <div class="card-body">
        <p>Crea un nuevo torneo y configura sus parámetros.</p>
        <div class="d-grid">
          <a href="/torneos/crear" class="btn btn-outline-success">Crear Nuevo Torneo</a>
        </div>
      </div>
    </div>
  </div>

  <div class="col-md-6 mb-4">
    <div class="card shadow h-100">
      <div class="card-header bg-info text-white">
        <h4 class="mb-0"><i class="fas fa-dumbbell me-2"></i> Mis Entrenamientos</h4>
      </div>
      <div class="card-body">
        <p>Gestiona tus entrenamientos personales y lleva un registro de tu progreso.</p>
        <div class="d-grid">
          <a href="/entrenamientos" class="btn btn-outline-info">Ver Mis Entrenamientos</a>
        </div>
      </div>
    </div>
  </div>

  <!-- Card para Estadísticas o Entrenamientos -->
  <div class="col-md-6 col-lg-4 mb-4">
    <div class="card h-100 shadow">
      <div class="card-header bg-primary text-white">
        <h5 class="mb-0"><i class="fas fa-chart-line me-2"></i> Evolución de Entrenamientos</h5>
      </div>
      <div class="card-body d-flex flex-column">
        <p class="card-text mb-3">
          Visualiza tu progreso a lo largo del tiempo en todos tus entrenamientos, analiza tu evolución y mejora tu rendimiento.
        </p>
        <div class="mt-auto">
          <a href="/entrenamientos/evolucion" class="btn btn-outline-primary w-100">
            <i class="fas fa-chart-line me-2"></i> Ver Mi Evolución
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Card para Logros y Nivel -->
  <div class="col-md-6 col-lg-4 mb-4">
    <div class="card h-100 shadow border-warning">
      <div class="card-header bg-warning text-dark">
        <div class="d-flex justify-content-between align-items-center">
          <h5 class="mb-0"><i class="fas fa-trophy me-2"></i> Logros y Nivel</h5>
          <span class="badge rounded-pill bg-primary">Nivel <%= user.nivel %></span>
        </div>
      </div>
      <div class="card-body d-flex flex-column">
        <div class="mb-3">
          <div class="d-flex justify-content-between mb-1">
            <small>Experiencia</small>
            <small><%= user.experiencia %> / <%= user.experiencia_necesaria %></small>
          </div>
          <div class="progress" style="height: 10px;">
            <div class="progress-bar bg-primary progress-bar-striped progress-bar-animated" 
                role="progressbar" 
                style="width:<%= Math.min((user.experiencia / user.experiencia_necesaria) * 100, 100) %>%;" 
                aria-valuenow="<%= Math.min((user.experiencia / user.experiencia_necesaria) * 100, 100) %>" 
                aria-valuemin="0" 
                aria-valuemax="100"></div>
          </div>
        </div>
        
        <p class="card-text mb-3">
          Desbloquea logros completando entrenamientos y mejorando tu precisión. Gana experiencia y sube de nivel para demostrar tu maestría en el tiro con arco.
        </p>
        
        <div class="mt-auto">
          <a href="/users/logros" class="btn btn-outline-warning w-100">
            <i class="fas fa-medal me-2"></i> Ver Mis Logros
          </a>
        </div>
      </div>
    </div>
  </div>

  <% if(user.isAdmin) { %>
    <div class="col-md-12 mb-4">
      <div class="card shadow">
        <div class="card-header bg-warning text-dark">
          <h4 class="mb-0"><i class="fas fa-users-cog me-2"></i> Administración</h4>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-6">
              <div class="d-grid">
                <a href="/users" class="btn btn-outline-warning mb-3">
                  <i class="fas fa-users me-2"></i> Gestionar Usuarios
                </a>
              </div>
            </div>
            <div class="col-md-6">
              <div class="d-grid">
                <a href="#" class="btn btn-outline-warning">
                  <i class="fas fa-trophy me-2"></i> Gestionar Torneos
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% } %>
</div> 